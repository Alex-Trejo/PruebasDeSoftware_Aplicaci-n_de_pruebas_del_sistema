# Variables de entorno para la aplicación de reservas
# Copia este archivo a .env y reemplaza los valores con tus credenciales reales

# Cadena de conexión a tu base de datos de MongoDB Atlas
MONGO_URI=mongodb+srv://<tu_usuario>:<tu_contraseña>@<tu_cluster>.mongodb.net/<tu_basededatos>

# Secreto para firmar los JSON Web Tokens (JWT)
# Puede ser cualquier cadena de texto larga y aleatoria
JWT_SECRET=tu_secreto_super_secreto_aqui